<%= form_with(model: product, class: "contents") do |form| %>
  <tr class="bg-indigo-50/30">
    <td class="px-6 py-4" colspan="4">
      <div class="flex flex-wrap items-end gap-4 p-2">
        <div class="flex-1 min-w-[200px]">
          <%= form.label :name, class: "block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1" %>
          <%= form.text_field :name, class: "block w-full rounded-lg border-slate-200 text-sm focus:border-indigo-primary focus:ring-indigo-primary" %>
        </div>

        <div class="flex-1 min-w-[250px]">
          <%= form.label :description, class: "block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1" %>
          <%= form.text_field :description, class: "block w-full rounded-lg border-slate-200 text-sm focus:border-indigo-primary focus:ring-indigo-primary" %>
        </div>

        <div class="w-32">
          <%= form.label :price, class: "block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1" %>
          <%= form.number_field :price, step: 0.01, class: "block w-full rounded-lg border-slate-200 text-sm focus:border-indigo-primary focus:ring-indigo-primary" %>
        </div>

        <div class="w-24">
          <%= form.label :quantity, class: "block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1" %>
          <%= form.number_field :quantity, class: "block w-full rounded-lg border-slate-200 text-sm focus:border-indigo-primary focus:ring-indigo-primary" %>
        </div>

        <div class="w-24">
          <%= form.label :threshold, "Low Stock", class: "block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1" %>
          <%= form.number_field :low_stock_threshold, class: "block w-full rounded-lg border-slate-200 text-sm focus:border-indigo-primary focus:ring-indigo-primary" %>
        </div>

        <div class="flex-1 min-w-[200px]">
          <%= form.label :image, class: "block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1" %>
          <%= form.file_field :image, class: "block w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-indigo-50 file:text-indigo-primary hover:file:bg-indigo-100" %>
        </div>

        <div class="flex items-center gap-2">
          <%= form.submit "Save", class: "px-4 py-2 bg-indigo-primary text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 cursor-pointer shadow-sm" %>
          <%= link_to "Cancel", products_path, class: "px-4 py-2 text-slate-600 text-sm font-medium hover:text-slate-900" %>
        </div>
      </div>

      <% if product.errors.any? %>
        <div id="error_explanation" class="mt-2 px-2 text-rose-danger text-xs font-medium">
          <ul>
            <% product.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
    </td>
  </tr>
<% end %>
